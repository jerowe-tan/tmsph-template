# App
APP_BASE_URL=http://localhost:3000

# Legacy (Auth0) — do not add anymore. Kept for reference only.
# AUTH0_DOMAIN=yourDomainFromAuth0
# AUTH0_CLIENT_ID=yourClientIdFromAuth0
# AUTH0_CLIENT_SECRET=yourClientSecretFromAuth0
# AUTH0_SECRET=generate-yours-with-node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
